<%= form_for(@global_resource) do |f| %>
  <% if @global_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@global_resource.errors.count, "error") %> prohibited this global_resource from being saved:</h2>

      <ul>
      <% @global_resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
